{
  "entities": {
    "Lead": {
      "title": "Lead",
      "description": "A potential customer lead captured from the contact form.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "The full name of the lead."
        },
        "email": {
          "type": "string",
          "description": "The email address of the lead.",
          "format": "email"
        },
        "details": {
          "type": "string",
          "description": "The project details provided by the lead."
        },
        "submittedAt": {
          "type": "string",
          "description": "The timestamp when the lead was submitted.",
          "format": "date-time"
        }
      },
      "required": [
        "name",
        "email",
        "details",
        "submittedAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "anonymous"
    ]
  },
  "firestore": {
    "/leads/{leadId}": {
      "schema": {
        "$ref": "#/entities/Lead"
      },
      "description": "Stores lead information from the contact form."
    }
  }
}
